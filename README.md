cyclicJSONSerialization
=======================

Experiments on serializing JSON data with cyclic references and building back from thus serialized data

Load `cyclicJSON.html`, open Console and run `runDemo()` to view the demo.
